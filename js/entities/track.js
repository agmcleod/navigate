(function() {
  var variableWidth = 200;
  game.Track = me.Renderable.extend({
    init : function() {
    },

    draw : function(context) {
      context.save();

      context.restore();
    },

    update : function(context) {

    }
  });
}).call(this);
